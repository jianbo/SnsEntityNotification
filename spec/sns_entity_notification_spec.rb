require "spec_helper"

describe SnsEntityNotification do
  it "has a version number" do
    expect(SnsEntityNotification::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
